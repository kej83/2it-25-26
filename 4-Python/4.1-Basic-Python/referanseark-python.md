# 🐍 Python – Kort Referanseark (for Øvehefte 1)

Dette arket viser kun **syntaksen** dere trenger i oppgavene.

---

## 📌 Variabler
```python
navn = "Anna"
alder = 16
tall = 3.14
````

---

## 📌 print

```python
print("tekst")
print("Tekst:", variabel)
```

---

## 📌 input

```python
navn = input("Skriv inn navn: ")
tall = int(input("Skriv inn heltall: "))
desimal = float(input("Skriv inn desimaltall: "))
```

---

## 📌 Grunnleggende operatorer

```python
+   -   *   /   //   %   **
```

---

## 📌 Sammenligning

```python
==   !=   >   <   >=   <=
```

---

## 📌 If / elif / else

```python
if betingelse:
    ...
elif annen_betingelse:
    ...
else:
    ...
```

---

## 📌 Logiske operatorer

```python
and
or
not
```

---

## 📌 for-løkker og range

```python
for i in range(stopp):
    ...

for i in range(start, stopp):
    ...

for i in range(start, stopp, steg):
    ...
```

---

## 📌 while-løkker

```python
while betingelse:
    ...
```

---

## 📌 Funksjoner

```python
def funksjon_navn():
    ...

def funksjon_navn(parameter):
    ...

def funksjon_navn(a, b):
    return verdi
```

---

## 📌 Lister

```python
liste = [1, 2, 3]

liste[0]        # hente element
liste.append(x) # legge til
len(liste)      # lengde
```

Løkke gjennom liste:

```python
for element in liste:
    ...

for i in range(len(liste)):
    ...
```

---

## 📌 Kommentarer

```python
# Dette er en kommentar
```
